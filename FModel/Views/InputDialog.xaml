<adonisControls:AdonisWindow x:Class="FModel.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        mc:Ignorable="d"
        Title="Input" Height="150" Width="300"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Enter the snapshot version:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="ResponseTextBox" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Click="OKButton_Click" IsDefault="True" MinWidth="60" />
            <Button Content="Cancel" IsCancel="True" MinWidth="60" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</adonisControls:AdonisWindow>